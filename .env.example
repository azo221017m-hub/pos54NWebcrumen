# Variables de entorno para Frontend

# URL de la API backend (SIN /api al final - se agrega automáticamente)
# También se usa para conexión WebSocket
VITE_API_URL=http://localhost:3000

# Nombre de la aplicación
VITE_APP_NAME=Web POS Crumen

# Timeout para peticiones API (ms)
VITE_API_TIMEOUT=10000

# Modo debug
VITE_DEBUG=true

# NOTA: La conexión WebSocket (Socket.IO) usa la misma URL que VITE_API_URL
# El sistema automáticamente establece la conexión WebSocket al iniciar la aplicación
# Las actualizaciones en tiempo real funcionan automáticamente sin necesidad de polling
